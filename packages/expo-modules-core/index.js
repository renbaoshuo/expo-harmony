'use strict';

// This package intentionally owns no Expo Modules JavaScript API. Applications
// import the canonical `expo-modules-core` package; RNOH autolinking consumes
// the Harmony metadata and HAR published here.
module.exports = Object.freeze({});
