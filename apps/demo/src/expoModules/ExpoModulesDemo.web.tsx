import { Note, Panel } from '../ui';

export function ExpoModulesDemo() {
  return (
    <Panel title="原生模块测试">
      <Note>请在 Android、iOS 或 HarmonyOS 开发构建中打开此页。浏览器不具备这些原生模块与原生页面。</Note>
    </Panel>
  );
}
